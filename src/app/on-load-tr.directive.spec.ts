import { OnLoadTrDirective } from './on-load-tr.directive';

describe('OnLoadTrDirective', () => {
  it('should create an instance', () => {
    const directive = new OnLoadTrDirective();
    expect(directive).toBeTruthy();
  });
});
